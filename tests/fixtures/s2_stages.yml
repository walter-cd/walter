namespace: s2

stages:
  - def:
      name: foo
      command: echo "hello foo in s2"
  - def:
      name: bar
      command: echo "hello bar in s2"
  - def:
      name: baz
      command: echo "hello baz in s2"
